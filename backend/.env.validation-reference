/**
 * Environment Variables Quick Reference
 * 
 * This file lists all environment variables validated by the application.
 * Copy from .env.example and replace placeholder values with real credentials.
 */

// ============================================
// REQUIRED - Application will not start without these
// ============================================

// Environment Configuration
NODE_ENV=development | production | test | staging (default: development)

// Database
DATABASE_HOST=<your-postgres-host>          // Required
DATABASE_PORT=5432                           // Default: 5432
DATABASE_USER=<your-db-user>                // Required
DATABASE_PASSWORD=<your-db-password>        // Required
DATABASE_NAME=<your-db-name>                // Required

// Redis
REDIS_HOST=<your-redis-host>                // Required
REDIS_PORT=6379                              // Default: 6379

// JWT Authentication
JWT_SECRET=<minimum-32-character-secret>    // Required, min 32 chars
                                            // Cannot be: 'your-secret-key', 'your-secret-key-change-in-production'

// S3 Storage
S3_ENDPOINT=<s3-endpoint>                   // Required, e.g., s3.timeweb.cloud
S3_REGION=<s3-region>                       // Required, e.g., ru-1
S3_BUCKET=<bucket-name>                     // Required
S3_ACCESS_KEY=<access-key>                  // Required, cannot be 'your-access-key'
S3_SECRET_KEY=<secret-key>                  // Required, cannot be 'your-secret-key'
S3_PUBLIC_URL=<https://bucket.url>          // Required, must be valid URL

// AI Services
OPENAI_API_KEY=<sk-...>                     // Required, cannot be 'your-openai-key'
GEMINI_API_KEY=<your-key>                   // Required, cannot be 'your-gemini-key'
PIAPI_API_KEY=<your-key>                    // Required, cannot be 'your-piapi-key'
KLING_API_KEY=<your-key>                    // Required, cannot be 'your-kling-key'
PHOTOROOM_API_KEY=<your-key>                // Required, cannot be 'your-photoroom-key'

// Google OAuth
GOOGLE_CLIENT_ID=<client-id>                // Required, cannot be 'your-google-client-id'
GOOGLE_CLIENT_SECRET=<client-secret>        // Required, cannot be 'your-google-client-secret'
GOOGLE_CALLBACK_URL=<http://localhost:3001/auth/google/callback> // Required, must be valid URL

// Frontend
FRONTEND_URL=<http://localhost:3000>        // Required, must be valid URL

// ============================================
// OPTIONAL - Defaults provided
// ============================================

// Video Generation
VIDEO_POLL_DELAY_MS=10000                   // Default: 10000, min: 1000
VIDEO_MAX_POLL_ATTEMPTS=30                  // Default: 30, min: 1

// Remotion
REMOTION_BUNDLE_PATH=./remotion-build // Default: ./remotion-build
REMOTION_OUTPUT_DIR=./output                // Default: ./output
REMOTION_COMPOSITION_ID=WbClassic           // Default: WbClassic

// Rate Limiting
THROTTLE_TTL=60000                          // Default: 60000, min: 1000
THROTTLE_LIMIT=10                           // Default: 10, min: 1

// Proxy (all optional)
PROXY_HOST=                                 // Optional
PROXY_PORT=                                 // Optional, must be valid port if set
PROXY_USER=                                 // Optional
PROXY_PASSWORD=                             // Optional

// ============================================
// VALIDATION RULES
// ============================================

/**
 * JWT_SECRET validation:
 * - Minimum 32 characters
 * - Cannot contain placeholder values
 * - Should be cryptographically random
 * 
 * Generate a secure secret:
 * node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
 */

/**
 * API Keys validation:
 * - Cannot be placeholder values (your-*, mock, etc.)
 * - Must be obtained from respective service providers
 */

/**
 * URL validation:
 * - Must be valid URI format
 * - Should include protocol (http:// or https://)
 */

/**
 * Port validation:
 * - Must be valid port number (1-65535)
 */
